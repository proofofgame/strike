{
  "name": "randomness",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:report": "vitest run --coverage",
    "test:ci": "vitest run --coverage -- --coverage --costs"
  },
  "devDependencies": {
    "@stacks/clarinet-sdk": "^3.16.0",
    "@stacks/transactions": "^7.4.0",
    "@vitest/coverage-v8": "^1.6.1",
    "vitest": "^1.6.0",
    "vitest-environment-clarinet": "^3.0.2"
  }
}
